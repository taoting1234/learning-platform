获取指定的用户信息
---
parameters:
  - in: path
    name: user_id
    type: integer
    description: 用户id
    required: true
responses:
  200:
    description: 指定的用户信息
    schema:
      id: User
      properties:
        id:
          type: integer
          description: 用户id
        username:
          type: string
          description: 用户名